# Cookie
 If the variable contains a string, it displays that Zach ate the last cookie. If the number is an integer or floating point, it displays that Monica ate the last cookie. If the variable contains anything else, it displays that the dog ate the last cookie.
